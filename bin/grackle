#!/usr/bin/env lua
require "grackle"
if not arg[1] then
  error("Usage: grackle <directory name> (--preview)")
else
  grackle.generate_site(arg[1])
end

if arg[2] == "--preview" then
  os.execute("wsapi site")
end
